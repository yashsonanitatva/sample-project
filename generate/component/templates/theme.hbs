import { createComponentTheme } from '@theme/createComponentTheme';
import { tokens } from '@theme/tokens';

const { palette } = tokens;

type {{pascalCase name}}Theme = {
  backgroundColor: string;
};

const {{camelCase name}}Theme = createComponentTheme<{{pascalCase name}}Theme>({
  'dark': {
    backgroundColor: palette.mango[0],
  },
  'light': {
    backgroundColor: palette.skyBlue[9],
  },
});

export { {{camelCase name}}Theme };
