import styled from 'styled-components';

import { {{camelCase name}}Theme } from './{{pascalCase name}}.theme';

export const {{pascalCase name}}Container = styled.div`
  background-color: ${ {{camelCase name}}Theme.backgroundColor};
`;
