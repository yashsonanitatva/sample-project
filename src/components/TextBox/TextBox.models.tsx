export interface TextBoxProps {
  /**
   * Always add js docs comments so storybook creates auto docs  for us
   **/
  firstProp?: string;
  /**
   * Always add js docs comments so storybook creates auto docs  for us
   **/
  secondProp?: string;
}
